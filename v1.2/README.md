# v1.2 — language toggle

Bilingual UI.
- 中文 / EN toggle in the header, defaults to Chinese
- All UI, menu, overlays, canvas HUD, and tilt indicator translated
- Language choice persisted in localStorage

Open `index.html` to play this version.
